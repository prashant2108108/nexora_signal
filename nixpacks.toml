[phases.setup]
nixPkgs = ["python311Full", "curl"]

[phases.install]
cmds = [
  "curl -sS https://bootstrap.pypa.io/get-pip.py | python3",
  "python3 -m pip install -r requirements.txt"
]

[start]
cmd = "cd crawler && python3 start.py"